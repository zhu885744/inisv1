# EXP API 文档

## 接口概述

EXP 控制器负责用户经验值管理，支持经验值的增删改查、统计聚合，以及点赞、分享、收藏、签到等互动功能。

**接口类型**：基础接口 + 业务接口

### 经验值规则

经验值规则通过 `SYSTEM_EXP_RULES` 配置项进行管理，支持自定义配置。以下为默认规则：

| 操作类型 | 名称 | 单次经验值 | 每日限制次数 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| `like` | 点赞 | 1 | 10 | 点赞文章/页面/评论/动态 |
| `collect` | 收藏 | 1 | 10 | 收藏文章/页面 |
| `visit` | 访问 | 1 | 10 | 访问文章/页面 |
| `share` | 分享 | 1 | 10 | 分享文章/页面/动态 |
| `login` | 登录 | 5 | 1 | 每日首次登录 |
| `comment` | 评论 | 1 | 10 | 发表评论 |
| `check-in` | 签到 | 10 | 1 | 每日签到 |
| `moments` | 发布动态 | 50 | 1 | 发布动态（自动触发） |

**配置方式**：管理员可通过配置 API 修改 `SYSTEM_EXP_RULES` 的 JSON 值来调整规则，修改后立即生效。详细配置说明请参考 [Config API 文档](config.md) 中的 `SYSTEM_EXP_RULES` 配置项。

---

## 状态码规范

| 状态码 | 使用场景 | 开发注意事项 |
|--------|----------|--------------|
| 200 | 接口调用成功且数据返回正常 | 所有正常响应必须返回200，data为有效业务数据 |
| 202 | 业务逻辑处理完成但有特殊状态 | 如重复签到、重复点赞等 |
| 204 | 接口调用成功但无数据返回 | 空数据场景必须返回204 |
| 400 | 请求参数错误或业务校验失败 | 参数校验失败、必填字段缺失 |
| 401 | 未登录或登录态失效 | 用户未登录时返回 |
| 403 | 无权限访问 | 非root用户访问他人数据时返回 |
| 405 | 请求方法不允许 | 调用了未定义的method |
| 500 | 服务器异常 | 生产环境屏蔽具体错误 |

---

## 接口列表

### 基础接口

#### 1. 获取指定记录 [基础接口-获取指定]

**请求方式**：GET  
**请求路径**：`/api/exp/one`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | int | 是 | 记录ID |
| field | string | 否 | 字段过滤 |

#### 2. 获取记录列表 [基础接口-获取全部]

**请求方式**：GET  
**请求路径**：`/api/exp/all`

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| field | string | 否 | - | 字段过滤 |
| where | string | 否 | - | 条件查询 |
| order | string | 否 | create_time desc | 排序 |

#### 3. 随机获取 [基础接口-随机获取]

**请求方式**：GET  
**请求路径**：`/api/exp/rand`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| except | string | 否 | 排除ID列表 |
| limit | int | 否 | 返回数量限制 |

#### 4. 查询数量 [基础接口-查询数量]

**请求方式**：GET  
**请求路径**：`/api/exp/count`

#### 5. 求和 [基础接口-求和]

**请求方式**：GET  
**请求路径**：`/api/exp/sum`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| field | string | 是 | 要求和的字段 |

#### 6. 最小值 [基础接口-最小值]

**请求方式**：GET  
**请求路径**：`/api/exp/min`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| field | string | 是 | 要求最小值的字段 |

#### 7. 最大值 [基础接口-最大值]

**请求方式**：GET  
**请求路径**：`/api/exp/max`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| field | string | 是 | 要求最大值的字段 |

#### 8. 查询列 [基础接口-查询列]

**请求方式**：GET  
**请求路径**：`/api/exp/column`

#### 9. 添加记录 [基础接口-添加数据]

**请求方式**：POST  
**请求路径**：`/api/exp/create`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| value | int | 是 | 经验值 |
| type | string | 是 | 类型（like/share/collect/check-in等） |
| description | string | 否 | 描述 |

#### 10. 修改记录 [基础接口-修改数据]

**请求方式**：PUT  
**请求路径**：`/api/exp/update`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | int | 是 | 记录ID |
| value | int | 否 | 经验值 |
| type | string | 否 | 类型 |

#### 11. 保存记录 [基础接口-保存数据]

**请求方式**：POST  
**请求路径**：`/api/exp/save`

#### 12. 软删除 [基础接口-软删除]

**请求方式**：DELETE  
**请求路径**：`/api/exp/remove`

#### 13. 彻底删除 [基础接口-彻底删除]

**请求方式**：DELETE  
**请求路径**：`/api/exp/delete`

#### 14. 清空回收站 [基础接口-清空回收站]

**请求方式**：DELETE  
**请求路径**：`/api/exp/clear`

#### 15. 恢复数据 [基础接口-恢复数据]

**请求方式**：PUT  
**请求路径**：`/api/exp/restore`

---

### 业务接口

#### 16. 签到 [业务接口]

**请求方式**：POST  
**请求路径**：`/api/exp/check-in`

**说明**：用户每日签到获取经验值，每日仅可签到一次，重复签到返回 202 状态码。经验值数量由 `SYSTEM_EXP_RULES` 配置项决定（默认为 +10 EXP）。

**响应示例**：

```json
{
  "code": 200,
  "msg": "签到成功！",
  "data": {
    "value": 10
  }
}
```

**重复签到响应**：

```json
{
  "code": 202,
  "msg": "今天已经签到过了！",
  "data": {
    "value": 0
  }
}
```

#### 17. 签到状态 [业务接口]

**请求方式**：GET  
**请求路径**：`/api/exp/check-in-status`

**说明**：查询当前用户今日签到状态及连续签到天数（需登录）

**响应字段**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| checked | bool | 今天是否已签到 |
| value | int | 签到获得的经验值（未签到时为 0） |
| check_in_time | int64 | 签到时间戳（未签到时为 0） |
| streak | int | 连续签到天数（从今天往回数，最多 365 天） |
| today | int64 | 今天 0 点的时间戳 |

**响应示例**：

```json
{
  "code": 200,
  "msg": "查询成功！",
  "data": {
    "checked": true,
    "value": 10,
    "check_in_time": 1750982400,
    "streak": 7,
    "today": 1750953600
  }
}
```

#### 18. 签到排行榜 [业务接口]

**请求方式**：GET  
**请求路径**：`/api/exp/check-in-rank`

**说明**：获取指定时间范围内签到次数最多的用户排行（公开接口，无需登录）

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| start | int64 | 否 | 本月1号 00:00 | 查询起始时间戳 |
| end | int64 | 否 | 本月末 23:59:59 | 查询结束时间戳 |
| limit | int | 否 | 系统配置 | 返回数量限制 |

**响应字段**（数组项）：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| rank | int | 排名（从 1 开始） |
| id | int | 用户 ID |
| nickname | string | 用户昵称 |
| avatar | string | 用户头像 |
| check_in_count | int | 签到次数 |
| total_exp | int | 签到获得的总经验值 |

**响应示例**：

```json
{
  "code": 200,
  "msg": "数据请求成功！",
  "data": [
    {
      "rank": 1,
      "id": 1,
      "nickname": "admin",
      "avatar": "",
      "check_in_count": 25,
      "total_exp": 250
    },
    {
      "rank": 2,
      "id": 2,
      "nickname": "test",
      "avatar": "",
      "check_in_count": 20,
      "total_exp": 200
    }
  ]
}
```

#### 19. 点赞 [业务接口]

**请求方式**：POST  
**请求路径**：`/api/exp/like`

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| bind_type | string | 否 | article | 类型（article/page/comment/moments） |
| bind_id | int | 是 | - | 绑定ID |
| state | int | 否 | 1 | 状态（0取消/1点赞） |

**说明**：点赞获取经验值，经验值数量由 `SYSTEM_EXP_RULES` 配置项决定（默认为 +1 EXP）。同一对象每日仅可点赞一次。

**响应示例**：

```json
{
  "code": 200,
  "msg": "点赞成功！",
  "data": {
    "value": 1
  }
}
```

#### 20. 分享 [业务接口]

**请求方式**：POST  
**请求路径**：`/api/exp/share`

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| bind_type | string | 否 | article | 类型（article/page/moments） |
| bind_id | int | 是 | - | 绑定ID |

**说明**：分享获取经验值，经验值数量由 `SYSTEM_EXP_RULES` 配置项决定（默认为 +1 EXP）。每日限制次数由配置决定（默认10次）。

**响应示例**：

```json
{
  "code": 200,
  "msg": "分享成功！",
  "data": {
    "value": 1
  }
}
```

#### 21. 收藏 [业务接口]

**请求方式**：POST  
**请求路径**：`/api/exp/collect`

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| bind_type | string | 否 | article | 类型（article/page/moments） |
| bind_id | int | 是 | - | 绑定ID |
| state | int | 否 | 1 | 状态（0取消/1收藏） |

**说明**：收藏获取经验值，经验值数量由 `SYSTEM_EXP_RULES` 配置项决定（默认为 +1 EXP）。同一对象每日仅可收藏一次。

**响应示例**：

```json
{
  "code": 200,
  "msg": "收藏成功！",
  "data": {
    "value": 1
  }
}
```

#### 22. 活跃用户排行 [业务接口]

**请求方式**：GET  
**请求路径**：`/api/exp/active`

**说明**：获取指定时间范围内经验值最多的用户排行

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| start | int | 否 | 开始时间戳（默认本月初） |
| end | int | 否 | 结束时间戳（默认本月末） |

**响应示例**：

```json
{
  "code": 200,
  "msg": "数据请求成功！",
  "data": [
    {
      "id": 1,
      "nickname": "用户A",
      "avatar": "...",
      "exp": 1500,
      "count": 25
    }
  ]
}
```

---

## 特殊说明

### 经验值规则配置

经验值规则现已支持通过 `SYSTEM_EXP_RULES` 配置项进行自定义配置，不再硬编码。配置项包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | string | 操作名称（用于显示） |
| `value` | int | 单次操作获得的经验值 |
| `daily_limit` | int | 每日限制次数 |

**配置示例**：
```json
{
  "like": {"name": "点赞", "value": 1, "daily_limit": 10},
  "check-in": {"name": "签到", "value": 10, "daily_limit": 1},
  "moments": {"name": "发布动态", "value": 50, "daily_limit": 1}
}
```

### 权限控制

- 普通用户只能操作自己的数据
- root用户可以操作所有数据

### 缓存机制

- 查询接口支持缓存
- 配置更新后自动清除经验值配置缓存（通过 Config 控制器的 `delCache()` 方法）
- 数据更新后自动清除相关缓存